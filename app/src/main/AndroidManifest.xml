<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.coco">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PhrasesActivity"
            android:label="@string/PhrasesActivity_activity_title"></activity>
        <activity android:name=".ColorsActivity"
            android:label="@string/ColorsActivity_activity_title" />
        <activity android:name=".FamilyActivity"
            android:label="@string/FamilyActivity_activity_title" />
        <activity android:name=".NumbersActivity"
            android:label="@string/NumbersActivity_activity_title"/>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>